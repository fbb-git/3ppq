#include <iostream>
#include <fstream>

#include "../tools.h"

using namespace std;

int main()
{
    fstream io("driver.2", ios::in | ios::out);

    Tools::enlarge(io, 200, 10);

//    io.seekp(200).write(hel;
//    io.write(hello.c_str(), hello.size());

    Tools::reduce("driver.2", 200, 10);
//    Tools::reduce(io, 200, 10);
}



